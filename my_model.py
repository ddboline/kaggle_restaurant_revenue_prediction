#!/usr/bin/python

import os

import cPickle as pickle
import gzip

import numpy as np

def score_model(model, xtrain, ytrain):
    return

def prepare_submission(xtrain, ytrain, xtest, ytest):
    return

if __name__ == '__main__':
    xtrain, ytrain, xtest, ytest = load_data()
